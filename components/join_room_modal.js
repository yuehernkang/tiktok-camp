import Modal from './../node_modules/@mui/material/Modal/Modal.d';

export const defaultRoomModal = (state, onOpen, onClose) => {
    return (
            <Modal>
                Hello
            </Modal>
        )
}